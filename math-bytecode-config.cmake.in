# Compute paths
get_filename_component(math-bytecode_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

# Our library dependencies (contains definitions for IMPORTED targets)
if(NOT TARGET runtime-compiler AND NOT math-bytecode_BINARY_DIR)
  include("${math-bytecode_CMAKE_DIR}/math-bytecode-targets.cmake")
endif()
